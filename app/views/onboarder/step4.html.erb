<% @years.each do |year| %>
  <%= link_to tour_path(step: @step+1, institution: @institution, course: @course, year: year), class: 'btn' do %>
    <div>
      År <%= year %>.
    </div>
  <% end %>
<% end %>
<%= link_to "Tillbaka", tour_path(step: @step-1), class: 'btn warning' %>
